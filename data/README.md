# Data Foundation

This folder contains the safe data contract for RoleAxis.

- `schemas/` defines the structures the product uses.
- `templates/` provides non-sensitive starter records, question libraries, and intake question sets for development.

Real user records belong in `local-data/`, which is intentionally ignored by Git.
